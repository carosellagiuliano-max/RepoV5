# Stripe webhook redirect
/api/webhooks/stripe  /.netlify/functions/stripe-webhook  200

# Netlify function redirects
/api/*  /.netlify/functions/:splat  200

# SPA fallback (must be last)
/*    /index.html   200